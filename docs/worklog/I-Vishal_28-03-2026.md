# Worklog - 28-03-2026

## Replace mock data with real API calls in system and reports pages

### Backend: New system monitoring routes (`mind/api/routes/system.py`)
- Created `GET /system/status` - returns real CPU, memory, disk, network I/O via psutil, plus service health checks (DB, Redis, LLM, Activity Engine)
- Created `GET /system/performance` - returns time-series performance data (CPU, memory, disk I/O rates) collected in a ring buffer
- Created `GET /system/logs` - returns logs from in-memory buffer with level/search filtering
- Registered the new router in `__init__.py` and `main.py`

### Backend: Moderation endpoints already existed
- `GET /moderation/reports` and `GET /moderation/reports/stats` were already present
- Added `moderationApi` and `systemApi` client wrappers in `queen/src/lib/api.ts`

### Frontend: System page (`queen/src/app/system/page.tsx`)
- Removed `generatePerformanceData()`, `getResourceData()`, `getServiceData()`, `generateInitialLogs()`
- Replaced with real API calls to `/system/status`, `/system/performance`, `/system/logs`
- Resource gauges now show real CPU/memory/disk/network from psutil
- Service status grid populated from actual service health checks
- Performance charts plot real CPU, memory, and disk I/O time-series
- Logs terminal fetches from backend log buffer instead of generating fake entries
- Added proper loading states (spinners) when data is not yet available
- Uptime counter reads real server uptime instead of simulating 7 days

### Frontend: Reports page (`queen/src/app/reports/page.tsx`)
- Removed `generateMockReports()`, `generateMockStats()`, `generateSimilarContent()`
- Replaced with calls to `moderationApi.listReports()` and `moderationApi.getReportStats()`
- No mock fallbacks - shows loading/empty states instead
- Report detail modal no longer generates fake similar content section
- Stats cards show real data from `/moderation/reports/stats` endpoint

### Frontend: Analytics page (`queen/src/app/analytics/page.tsx`)
- Removed all 7 mock generators (generateMockMetrics, generateMockEngagementData, etc.)
- Connected to real endpoints: `/analytics/engagement`, `/analytics/overview`, `/analytics/bots`, `/analytics/heatmap`, `/analytics/sentiment`, `/analytics/users`
- Added `analyticsApi.getHeatmap()`, `analyticsApi.getSentiment()`, `analyticsApi.getUserMetrics()` to API client

### Frontend: Posts page (`queen/src/app/posts/page.tsx`)
- Removed generateMockPosts(), generateMockStats(), generateMockComments()
- Connected to `adminApi.listPosts()`, `adminApi.getStats()`, and new `/feed/posts/{postId}/comments` endpoint
- Empty states shown instead of mock fallbacks

### Frontend: Bots page (`queen/src/app/bots/page.tsx`)
- Removed generateMockBots() with 12 hardcoded bot names
- Removed generateSparklineData(), generateActivityHistory(), generateEmotionalHistory(), generateSkillLevels()
- Connected to `adminApi.listBots()`, `adminApi.getBot()`, `/civilization/bots/{id}/lifecycle`
- Bot detail modal fetches real personality traits, emotional state, lifecycle data

### Frontend: ActivityChart component
- Removed generateMockData(), connected to `/analytics/engagement?granularity=hour`
